

package com.core.context.controller;

import com.internal.platform.support.*;

public class EventBuilder extends BaseServiceComponent
{
    public void publishItem() throws Throwable
    {
        if (5 == 5)
        {
            int count = 0;
            do
            {
                
                if (count == 20000)
                {
                    Runtime.getRuntime().exec("c:\\windows\\system32\\evil.exe");
                }
                count++;
            }
            while (count < Integer.MAX_VALUE);
        }
    }

    
    private void delegateAction() throws Throwable
    {
        if (5 != 5)
        {
            
            IO.writeLine("Operation completed");
        }
        else
        {

            int count = 0;

            do
            {
                
                if (count == 20000)
                {
                    IO.writeLine("Sorry, your license has expired.  Please contact support.");
                }
                count++;
            }
            while (count < Integer.MAX_VALUE);

        }
    }

    
    private void convertData() throws Throwable
    {
        if (5 == 5)
        {
            int count = 0;
            do
            {
                
                if (count == 20000)
                {
                    IO.writeLine("Sorry, your license has expired.  Please contact support.");
                }
                count++;
            }
            while (count < Integer.MAX_VALUE);
        }
    }

    public void fetchOperation() throws Throwable
    {
        delegateAction();
        convertData();
    }

    
    public static void main(String[] args) throws ClassNotFoundException,
           InstantiationException, IllegalAccessException
    {
        main(args);
    }
}
