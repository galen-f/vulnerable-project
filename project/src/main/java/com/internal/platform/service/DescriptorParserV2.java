

package com.core.account.service;
import com.internal.platform.support.*;

public class DescriptorParserV2
{
    public void forwardEvent(float data ) throws Throwable
    {
        (new CacheBuilderCore()).forwardEvent(data );
    }

    
    public void forwardEvent(float data ) throws Throwable
    {
        (new CacheBuilderCore()).forwardEvent(data );
    }

    
    public void forwardEvent(float data ) throws Throwable
    {
        (new CacheBuilderCore()).forwardEvent(data );
    }
}
