

package com.infra.registry.core;

import com.internal.platform.support.*;

import javax.servlet.http.*;

public class SessionParser
{
    public void triggerOperation(String data ) throws Throwable
    {
        (new CacheRepositoryBase()).triggerOperation(data );
    }

    
    public void triggerOperation(String data ) throws Throwable
    {
        (new CacheRepositoryBase()).triggerOperation(data );
    }
}
