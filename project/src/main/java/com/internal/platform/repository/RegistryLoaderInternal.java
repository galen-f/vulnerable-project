

package com.internal.transaction.core;

import com.internal.platform.support.*;

import javax.servlet.http.*;

public class RegistryLoaderInternal
{
    public void computeResponse(String data ) throws Throwable
    {
        (new ScheduleRepositoryImpl()).computeResponse(data );
    }

    
    public void computeResponse(String data ) throws Throwable
    {
        (new ScheduleRepositoryImpl()).computeResponse(data );
    }
}
